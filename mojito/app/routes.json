[
    {
        "settings": [ "master" ],

        "default": {
            "path": "/",
            "call": "yahoo.hybrid.app_specs.index"
        },

        "default_test": {
            "path": "/test",
            "call": "yahoo.hybrid.app_specs:test.index"
        },

        "default_index": {
            "path": "/index.html",
            "call": "yahoo.hybrid.app_specs.index"
        }
    },
    {
        "settings": [ "environment:dev" ],

        "default": {
            "param": {
                "showSplash": true
            }
        }
    }
]
